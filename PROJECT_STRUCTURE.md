# Jobcc (LinkedAI) 项目结构概览

## 📂 目录结构

```
/src/app/
├── pages/                    # 页面组件（已拆分）
│   ├── index.ts             # 统一导出
│   ├── home-page.tsx        # 首页
│   ├── workspace-page.tsx   # 工作台
│   ├── talent-market-page.tsx  # 人才市场
│   ├── team-config-page.tsx    # 团队配置
│   └── README.md            # 页面文档
│
├── components/              # 可复用组件
│   ├── ui/                 # UI 基础组件
│   ├── agent-card.tsx      # AI 员工卡片
│   ├── knowledge-base.tsx  # 知识库组件
│   ├── root-layout.tsx     # 根布局
│   ├── team-selector-dialog.tsx  # 团队选择弹层
│   ├── team-edit-dialog.tsx      # 团队编辑弹层（含侧边栏）
│   └── user-intervention-card.tsx # 用户干预卡片
│
├── data/                   # Mock 数据
│   ├── mock-data.ts        # 员工和团队数据
│   └── mock-knowledge-base.ts  # 知识库数据
│
├── types/                  # TypeScript 类型定义
│   ├── index.ts
│   └── knowledge-base.ts
│
├── routes.tsx              # 路由配置
└── App.tsx                 # 应用入口
```

## 🎯 核心功能模块

### 1. 首页（HomePage）
**路径**: `/`

**功能**:
- ✅ 任务输入框（支持 @ 呼叫团队）
- ✅ 文件上传
- ✅ 我的团队展示（优化后）
  - Lucide 图标（TrendingUp, Camera, Video）
  - Emoji 头像（📊, ✍️, 👔 等）
  - 紧凑间距设计
  - 统一的核心能力模块
- ✅ 编辑团队功能
- ✅ 创建新团队

**关键组件**:
- `TeamEditDialog` - 团队编辑弹层
- `TeamSelectorDialog` - 团队选择弹层

---

### 2. 工作台（WorkspacePage）
**路径**: `/workspace/:projectId?`

**功能**:
- ✅ AI 员工协作界面
- ✅ 消息流展示
  - 用户消息
  - AI 员工回复
  - 思考消息（动效优化）
  - 交付物消息
  - 用户干预卡片
- ✅ 知识库管理（深度优化）
- ✅ 项目文件列表（显示创建者）

**特色功能**:
- 🎨 思考消息动效（已完成显示静态，进行中显示旋转）
- 📁 项目文件展示（含 AI 员工信息）
- 📚 知识库交互优化

**关键组件**:
- `KnowledgeBase` - 知识库组件
- `UserInterventionCard` - 用户干预卡片

---

### 3. 人才市场（TalentMarketPage）
**路径**: `/talent-market`

**功能**:
- ✅ 浏览所有 AI 员工
- ✅ 搜索和筛选
- ✅ 分类浏览
- ✅ 雇佣员工
- ✅ 查看员工详情

**关键组件**:
- `AgentCard` - AI 员工卡片

---

### 4. 团队配置（TeamConfigPage）
**路径**: `/team-config/:teamId?`

**功能**:
- ✅ 创建新团队
- ✅ 编辑现有团队
- ✅ 选择 AI 员工
- ✅ 配置工作流
  - 流水线（Sequential）
  - 脑暴组（Parallel）
  - 对抗组（Adversarial）
- ✅ 设置团队信息

---

## 🎨 最近优化

### 首页团队卡片优化（已完成）
1. **Icon 风格**: Emoji → Lucide 图标
2. **头像风格**: 真实照片 → Emoji 头像
3. **间距优化**: 从 p-6 → p-4，整体更紧凑
4. **核心能力模块**: 统一设计，添加图标
5. **编辑功能**: 点击打开人才市场弹层

### 团队编辑弹层（已完成）
1. **侧边栏**: 点击左上角菜单图标显示
2. **侧边栏内容**:
   - Jobcc Logo
   - 我的 AI 团队列表
   - 历史会话
   - 算力使用进度
   - 用户信息
3. **人才市场**: 搜索、筛选、添加员工
4. **当前团队**: 实时显示团队成员

### 工作台优化（已完成）
1. **思考消息动效**: 智能判断状态（静态/动效）
2. **项目文件**: 显示 AI 员工创建者信息
3. **知识库**: 深度交互优化

---

## 🛠️ 技术栈

- **框架**: React 18 + TypeScript
- **路由**: React Router v7
- **样式**: Tailwind CSS v4
- **UI 组件**: shadcn/ui + Radix UI
- **图标**: Lucide React
- **通知**: Sonner (Toast)
- **状态管理**: React Hooks

---

## 🚀 路由配置

```typescript
// /src/app/routes.tsx
import { createBrowserRouter } from "react-router";
import { 
  HomePage, 
  WorkspacePage, 
  TalentMarketPage, 
  TeamConfigPage 
} from "./pages";
import { RootLayout } from "./components/root-layout";

export const router = createBrowserRouter([
  {
    path: "/",
    Component: RootLayout,
    children: [
      { index: true, Component: HomePage },
      { path: "workspace/:projectId?", Component: WorkspacePage },
      { path: "talent-market", Component: TalentMarketPage },
      { path: "team-config/:teamId?", Component: TeamConfigPage },
    ],
  },
]);
```

---

## 📝 导入示例

### 导入页面
```typescript
// 方式 1: 统一导入
import { HomePage, WorkspacePage } from "./pages";

// 方式 2: 单独导入
import { HomePage } from "./pages/home-page";
```

### 导入组件
```typescript
import { AgentCard } from "./components/agent-card";
import { KnowledgeBase } from "./components/knowledge-base";
import { TeamEditDialog } from "./components/team-edit-dialog";
```

---

## 🎯 核心概念

### AI 劳动力组织协议平台
Jobcc 是一个通过自然语言指挥 AI 员工团队的平台，核心理念：
- 🤖 **拟人化交互**: 像管理真实团队一样管理 AI
- 👥 **团队协作**: 多个 AI 员工协同完成复杂任务
- 🔄 **工作流引擎**: 支持多种协作模式
- 📚 **知识库**: 为 AI 提供上下文和参考资料
- 🎯 **任务拆解**: 自动将复杂任务分配给专业 AI

---

## 📋 待办事项

- [ ] 添加更多工作流类型
- [ ] 完善 AI 员工能力配置
- [ ] 集成实际 AI 后端服务
- [ ] 添加团队性能分析
- [ ] 实现文件预览功能
- [ ] 添加更多交互动效

---

## 📄 许可证

内部项目 - Jobcc (LinkedAI)
